# Azure Landing Zone - Terraform

Azure Landing Zone infrastructure as code following the Cloud Adoption Framework.

## Structure
## Deploy

`ash
cd environments/<environment>
terraform init
terraform plan
terraform apply
terraform destroy
`

## Requirements
- Terraform >= 1.3.0
- Azure CLI authenticated
- Azure Subscription ID
